// I am not sure about that, is it an object ? 
// I would not have put it there, as the isFav is really linked to the components logic
// I am not saying it is wrong, on the contrary it is interesting :)
export type favType = {
  favId: string;
  isFav: boolean;
};
